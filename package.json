{
  "name": "numbertool",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "号通查 - 号码标记自查向导、申诉材料生成器、法人号码核验预检与手机号归属地查询工具站",
  "scripts": {
    "prebuild": "node scripts/generate-og.mjs && node scripts/generate-favicon.mjs",
    "build": "node build.mjs",
    "assets": "node scripts/generate-og.mjs && node scripts/generate-favicon.mjs",
    "dev": "npm run build && node scripts/serve.mjs",
    "blog": "node scripts/build-blog.mjs",
    "verify": "python scripts/verify-build.py"
  },
  "engines": {
    "node": ">=18"
  }
}
